const r=require('express').Router(),auth=require('../middleware/auth.middleware'),allow=require('../middleware/authorize.middleware'),s=require('../services/flexibleWork.service');const out=(f,c=200)=>async(q,p)=>{try{p.status(c).json({success:true,data:await f(q)})}catch(e){p.status(400).json({success:false,message:e.message})}};r.get('/',auth,allow('OWNER','MANAGER','ACCOUNTANT'),out(q=>s.list(q.user)));r.post('/',auth,allow('ACCOUNTANT'),out(q=>s.create(q.body||{},q.user),201));module.exports=r;
